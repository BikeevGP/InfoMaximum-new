import { style } from "typestyle";


export const inputPasswordLayer = style({
    position:"relative"
  });
  export const eye = style({
    position:"absolute",
    top: "26px",
    right: "12px",
    cursor:"pointer"
  });